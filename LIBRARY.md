# 番库（本地动漫库）设计

> NagomiAni 的本地库模块：导入目录 → 自动识别匹配 Bangumi → 按路径播放 → 看完自动同步

## 总体闭环

```
导入目录 → 扫描索引 → 自动匹配 Bangumi 条目 → 库内点播（路径→集数）
                                        ↓
                        播完 → 自动标记该集"看过"（已完成）
```

## 1. 目录导入与索引

- **监控目录**：可配置多个文件夹（存 UserDefaults）
- **扫描**：遍历目录 → 收集视频文件（mkv/mp4/mov/webm 等）
- **分组**：按 `seriesKey`（文件名去集数标记 + 父文件夹名）聚合为「系列」
- **索引持久化**：JSON 存 `~/Library/Application Support/NagomiAni/library.json`
  - 结构：`seriesKey → { files: [{path, episode}], subjectID?, matchState }`
- **增量**：启动时扫描 + 手动刷新按钮；（后续用 FSEvents 监听目录自动发现新文件）

## 2. 自动匹配管线（核心难点）

1. **标题清洗**：去掉 `[]` 标签（字幕组/画质/年份/网站水印）、集数标记，得到候选标题
2. **搜索**：`POST /v0/search/subjects`（filter type=2），可能用多个关键词（原文件名、清洗后标题）
3. **匹配评分**：
   - **标题相似度**：中文名（nameCN）、日文名（name）、别名归一化后计算（编辑距离/ngram）
   - **年份验证**：文件名年份 vs subject 播出年份
   - **集数验证**：subject 总集数 vs 本地文件集数范围
   - **季度判断**：S01/S2、副标题（0、II、Second 等）对应不同条目
4. **三态结果**：
   - ✅ 自动确认（高分）
   - ⏳ 待确认（中分，用户在 UI 手动选择）
   - ❌ 未识别（低分，可手动搜索绑定）

## 3. 播放集成

- 库 UI：系列列表（封面墙 + 已看进度）→ 展开单集 → 双击播放（切到播放器页）
- 播放时：`seriesKey → subjectID` + 文件名集数 → 查该 subject 的 episodes 得到 episode_id（缓存）
- 播完自动同步：已实现（PATCH 单集收藏）

## 4. 补充思路（在用户方案之上）

- **断点续播**：本地记录每个文件的播放位置，重新打开自动从上次位置继续（配 Bangumi 同步进度）
- **反向同步**：对比 Bangumi 的 ep_status 与本地已看状态 →「还差第 X 集没看」的接着看提示
- **手动纠正**：匹配错误/漏匹配 → 右键「重新匹配 / 手动搜索 / 解除关联」
- **封面墙**：用 Bangumi subject 图片做系列封面，视觉化浏览
- **最近播放**：记录最后播放时间，库内按最近播放排序
- **目录监控**：FSEvents 监听，新文件自动入库（M4 后）

## 5. 分期

| 阶段 | 内容 | 验收 |
|---|---|---|
| L1 | 导入目录 + 扫描索引 + 系列/单集列表展示 | 拖入目录能看到番剧列表 |
| L2 | 自动匹配（清洗/搜索/评分）+ 三态 UI | 常见命名能自动匹配上条目 |
| L3 | 库内点播 + 播完自动同步（打通闭环） | 点一集→播放→看完→同步 |
| L4 | 断点续播 + 目录监控 + 手动纠正 + 封面墙 | 完整体验 |
